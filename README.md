# Medical Informatics

## Requirements

- Python (Anaconda or Miniconda)

- IDE (Spyder / Jupyter Lab / Vs Code, ...)

- Conda env:

  - ```
    conda create -n mi python=3.13
    conda activate mi
    pip install -r requirements
    ```

- Add env kernel to jupyter (inside the created conda env):

  - ```
    python -m ipykernel install --user --name mi --display-name "MI Env"
    ```

    > [!NOTE]
    >
    > You can get the list of jupyter kernels:
    >
    > ```
    > jupyter kernelspec list
    > ```
    >
    > You can remove the env from jupyter with the following command:
    >
    > ```
    > jupyter kernelspec remove mi
    > ```

---

## Run Codes

- For **notebooks**, run `jupyter lab` and select the `mi` kernel

- For `.py` **codes**, first activate the env, then run from cmd:

  - ```
    conda activate mi
    python filename.py
    ```

    > [!NOTE]
    >
    > You can use the created env in **Spyder** by changing the interpreter:
    >
    > - Tools --> Preferences --> Python Interpreter --> Select interpreter 
    >
    > - Restart the kernel or Spyder
    >
    > It will require you to install spyder-kernel:
    >
    > ```
    > pip install spyder-kernels==3.1.*
    > ```

---

## انواع داده های پزشکی و ویژگی های آن ها

در جدول زیر انواع داده‌های پزشکی که در مهندسی پزشکی با آن‌ها سروکار داریم، همراه با ویژگی‌ها، نحوه‌ی جمع‌آوری و ذخیره‌سازی آورده شده است:

| نوع داده                                     | مثال‌ها                     | ویژگی‌ها                       | نحوه‌ی جمع‌آوری                    | نحوه‌ی ذخیره‌سازی                                    |
| -------------------------------------------- | -------------------------- | ----------------------------- | -------------------------------- | -------------------------------------------------- |
| **داده‌ متنی (Textual Data)**                 | پرونده پزشکی، گزارش پزشک   | غیرساخت‌یافته، حجیم            | ورود دستی، سیستم HIS             | پایگاه داده رابطه‌ای (SQL)، فایل متنی، سیستم‌های HIS |
| **داده‌ عددی (Numerical Data)**               | فشار خون، دما، قند خون     | ساخت‌یافته، کم‌حجم              | سنسورها، دستگاه‌های مانیتورینگ    | پایگاه داده، فایل CSV، سیستم EMR                   |
| **داده‌ تصویری (Image Data)**                 | MRI، CT، X-Ray، Ultrasound | بسیار حجیم، نیازمند فشرده‌سازی | تجهیزات تصویربرداری پزشکی        | فرمت‌های DICOM، PACS سرور                           |
| **داده‌ سیگنالی (Signal Data)**               | ECG، EEG، EMG              | پیوسته، زمان‌محور              | الکترودها، سنسورهای بیولوژیکی    | فایل‌های EDF، MAT، HDF5                             |
| **داده‌ صوتی (Audio Data)**                   | صدای قلب، ریه، گفتار بیمار | زمان‌محور، نیازمند فیلترینگ    | میکروفن پزشکی، استتوسکوپ دیجیتال | فرمت WAV، FLAC یا پایگاه داده چندرسانه‌ای           |
| **داده‌ ویدئویی (Video Data)**                | اندوسکوپی، جراحی، حرکت بدن | حجیم، چندفریمی                | دوربین‌های پزشکی                  | MP4، AVI، DICOM-Video، PACS                        |
| **داده‌ ژنومیک (Genomic Data)**               | توالی DNA/RNA              | بسیار بزرگ، حساس              | توالی‌سنج‌ها (Sequencers)          | فایل FASTA، FASTQ، پایگاه داده Bioinformatics      |
| **داده‌ حسی/محیطی (Sensor/IoT Data)**         | دما، رطوبت، موقعیت بیمار   | بلادرنگ (Real-time)           | سنسورهای IoT، پوشیدنی‌ها          | پایگاه داده زمانی (Time-series DB)، MQTT Broker    |
| **داده‌ اداری/مدیریتی (Administrative Data)** | بیمه، هزینه درمان، نوبت‌دهی | ساخت‌یافته                     | سیستم‌های مدیریت بیمارستان        | پایگاه داده رابطه‌ای (SQL)، ERP سیستم‌ها             |

---



## کتابخانه های پایتون برای هر نوع داده

حتماً 👌
 در جدول زیر کتابخانه‌ها و ابزارهای پایتون برای **پردازش، نمایش و ذخیره‌سازی انواع داده‌های پزشکی** آورده شده‌اند:

| نوع داده                  | کتابخانه‌های پایتون مرتبط                                     | کاربرد اصلی                                           |
| ------------------------- | ------------------------------------------------------------ | ----------------------------------------------------- |
| **داده‌ متنی**             | **`pandas`**, `re`, `nltk`, `spacy`                          | تحلیل، پاک‌سازی، پردازش زبان طبیعی (NLP)               |
| **داده‌ عددی**             | `numpy`, **`pandas`**, `scipy`                               | تحلیل عددی، محاسبات آماری، ذخیره‌سازی در DataFrame     |
| **داده‌ تصویری (Image)**   | `pydicom`, `opencv`, `scikit-image`, `pillow`, `nibabel`, `SimpleITK` | خواندن/نوشتن تصاویر DICOM، پردازش تصویر پزشکی         |
| **داده‌ سیگنالی (Signal)** | `scipy.signal`, `mne`, `biosppy`, `neurokit2`                | فیلترکردن، استخراج ویژگی‌ها، تحلیل ECG/EEG             |
| **داده‌ صوتی (Audio)**     | `librosa`, `soundfile`, `scipy.io.wavfile`                   | تحلیل صدا، استخراج ویژگی‌های صوتی                      |
| **داده‌ ویدئویی (Video)**  | `opencv`, `imageio`, `moviepy`                               | پردازش ویدیو، فریم‌به‌فریم آنالیز، فشرده‌سازی            |
| **داده‌ ژنومیک (Genomic)** | `biopython`, **`pandas`**, `scikit-bio`                      | خواندن توالی‌های DNA/RNA، تحلیل بیوانفورماتیکی         |
| **داده‌ حسی/IoT**          | `paho-mqtt`, `influxdb-client`, `pyserial`, **`pandas`**     | دریافت داده از سنسورها، ذخیره‌سازی زمانی (time-series) |
| **داده‌ اداری/مدیریتی**    | `pandas`, `sqlalchemy`, `openpyxl`, `requests`               | اتصال به پایگاه‌داده‌ها، تبادل اطلاعات، گزارش‌گیری       |



### خواندن و نوشتن انواع فرمت ذخیره سازی سیگنال پزشکی

| قالب داده سیگنالی   | کتابخانه‌های مناسب خواندن/نوشتن         | توضیح                                                        |
| ------------------- | -------------------------------------- | ------------------------------------------------------------ |
| **CSV / TXT / TSV** | `pandas` (`read_csv`, `to_csv`)        | متداول‌ترین روش ذخیره‌سازی داده‌های زمان‌محور. سریع، ساده و سازگار با Excel. |
| **MAT (MATLAB)**    | `scipy.io.loadmat`, `scipy.io.savemat` | داده‌های EEG، EMG یا ECG که با MATLAB جمع‌آوری می‌شن.           |
| **EDF / BDF**       | `mne`, `pyedflib`                      | قالب استاندارد برای EEG و سیگنال‌های فیزیولوژیکی.             |
| **HDF5**            | `h5py`, `pandas.HDFStore`              | برای ذخیره‌سازی حجیم و چندسیگناله. بسیار سریع و ساخت‌یافته.    |
| **JSON**            | `pandas.read_json`, `json`             | برای ذخیره‌ی داده‌های ساخت‌یافته و سبک، معمولاً در APIها.        |
| **Numpy Binary**    | `numpy.save`, `numpy.load`             | سریع و مناسب برای داده‌های عددی خالص.                         |
| **Database**        | `pandas.to_sql`, `sqlalchemy`          | ذخیره و بازیابی سیگنال‌ها در پایگاه‌داده‌های SQL.               |

- `pandas` فوق‌العاده است برای **خواندن/نوشتن فایل‌های متداول (CSV, JSON, HDF5, SQL)**
- ولی برای **قالب‌های تخصصی سیگنال (EDF, MAT, BDF)** باید از کتابخانه‌های مخصوص استفاده بشه مثل `mne` یا `scipy.io`.



------

### خواندن و نوشتن انواع فرمت ذخیره‌سازی تصویر پزشکی

| قالب داده تصویری                 | کتابخانه‌های مناسب خواندن/نوشتن                   | توضیح                                                        |
| -------------------------------- | ------------------------------------------------ | ------------------------------------------------------------ |
| **DICOM (.dcm)**                 | `pydicom`, `dicom2nifti`, `SimpleITK`, `nibabel` | فرمت استاندارد تصاویر پزشکی (MRI، CT، X-ray). شامل متادیتا بیمار و تنظیمات دستگاه. |
| **NIfTI (.nii, .nii.gz)**        | `nibabel`, `SimpleITK`                           | فرمت رایج در MRI و fMRI برای ذخیره‌سازی حجم سه‌بعدی (3D/4D).   |
| **TIFF / TIF**                   | `PIL`, `imageio`, `tifffile`                     | برای تصاویر میکروسکوپی یا بافتی (histopathology). پشتیبانی از چند لایه (multi-page). |
| **PNG / JPG / JPEG**             | `opencv`, `PIL`, `imageio`                       | فرمت عمومی فشرده‌شده برای ذخیره تصاویر خروجی یا داده‌های آموزشی مدل‌ها. |
| **BMP**                          | `opencv`, `PIL`                                  | فرمت ساده و بدون فشرده‌سازی، مناسب برای کاربردهای پایه‌ای.     |
| **RAW / MetaImage (.mhd, .raw)** | `SimpleITK`, `nibabel`                           | داده خام تصویری که معمولاً همراه با فایل header ذخیره می‌شود (در تصویربرداری سه‌بعدی). |
| **HDF5**                         | `h5py`, `pandas.HDFStore`                        | برای ذخیره‌سازی حجم زیاد تصاویر به‌صورت ساخت‌یافته (مثلاً در آموزش شبکه‌های CNN). |
| **Numpy Binary (.npy, .npz)**    | `numpy.save`, `numpy.load`                       | برای ذخیره‌ی ماتریس‌های تصویر به صورت آرایه‌ی عددی در پروژه‌های یادگیری ماشین. |
| **DICOMDIR / PACS Server**       | `pydicom`, `dicomweb-client`                     | مدیریت و خواندن مجموعه‌ای از فایل‌های DICOM از سیستم‌های بیمارستانی. |

------

